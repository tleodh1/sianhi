/* Extend existing stage IDs and reward IDs; never migrate or reset saved progress. */
(function(global){
 const C=global.SianCurriculum,M=global.MissionModel,H=global.HanjaSpace;
 for(const subject of ['수학','사고력 수학'])C.curricula[subject]=C.curricula[subject].map(s=>{const d=M.descriptor(subject,s.stage),q=M.make(subject,s.stage);return {...s,title:d[0],learningObjective:d[1],story:'시안이가 '+d[0]+'의 작은 미션을 해결해요.',questionType:'MATH_MANIPULATIVE',conceptTags:[d[1].replace(/해요\./,''),'그림과 조작'],generator:'MissionModel.make',questionPool:d[2],explanation:q.explanation};});
 C.curricula['한자']=C.curricula['한자'].map(s=>{const l=H.lesson(s.stage),c=l.character;return {...s,title:c.character+' · '+c.meaning+' '+c.reading,learningObjective:l.review?'도움선을 줄여 획순과 모양을 기억해 써요.':'보기 → 듣기 → 따라쓰기 → 기억해서 쓰기 → 그림 연결',story:'그림에서 모양을 찾고 손으로 기억하는 한자 정원',questionType:'HANJA_TRACING',conceptTags:[c.strokeCount+'획',l.review?'기억 복습':'첫 쓰기'],generator:'HanjaSpace.lesson',questionPool:[c.character],explanation:c.meaning+' '+c.reading};});
 for(const subject of ['영어','과학'])C.curricula[subject]=C.curricula[subject].map(s=>{const q=global.DiscoveryModel.make(subject,s.stage);return {...s,title:q.title,learningObjective:q.prompt,story:'그림을 보고 듣고 직접 움직이는 '+q.title,questionType:'PICTURE_DISCOVERY',conceptTags:['그림·소리','직접 조작'],generator:'DiscoveryModel.make',questionPool:[q.mode],explanation:q.explanation};});
})(window);
